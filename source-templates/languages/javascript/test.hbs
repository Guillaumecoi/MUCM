// =============================================================================
// AUTO-GENERATED TEST DOCUMENTATION
// Use Case: {{title}} ({{id}})
{{#if description}}// Description: {{description}}
{{/if}}{{#if preconditions}}// Preconditions:
{{#each preconditions}}// - {{text}}
{{/each}}{{/if}}{{#if postconditions}}// Postconditions:
{{#each postconditions}}// - {{text}}
{{/each}}{{/if}}// Documentation: docs/use-cases/{{category}}/README.md
// Last Changed: {{date_format metadata.updated_at}}
// =============================================================================

{{#each scenarios}}
// ## Scenario: {{title}} ({{id}})
{{#if description}}// **Description:** {{description}}
{{/if}}
//
{{/each}}
// =============================================================================
// AUTO-GENERATED TEST CODE
// ⚠️  WARNING: Only modify code between START/END USER IMPLEMENTATION markers!
// =============================================================================

// =============================================================================
// START USER IMPLEMENTATION - Add your imports and setup code here
// =============================================================================

// Add your imports here:
// const { expect } = require('@jest/globals');
// const YourModule = require('./your-module');

// Add test helper functions here if needed

// =============================================================================
// END USER IMPLEMENTATION
// =============================================================================

/**
 * Test suite for use case: {{title}} ({{id}})
 * Category: {{category}}
{{#if description}} * Description: {{description}}
{{/if}} */
describe('{{title}}', () => {
    
    // =============================================================================
    // START USER IMPLEMENTATION - Add your setup and teardown code here
    // =============================================================================
    
    beforeEach(() => {
{{#if preconditions}}
        // Use case preconditions to consider:
{{#each preconditions}}
        // - {{text}}
{{/each}}
{{/if}}
        // TODO: Add setup code that runs before each test
    });
    
    afterEach(() => {
        // TODO: Add cleanup code that runs after each test
    });
    
    beforeAll(() => {
        // TODO: Add setup code that runs once before all tests
    });
    
    afterAll(() => {
        // TODO: Add cleanup code that runs once after all tests
    });
    
    // =============================================================================
    // END USER IMPLEMENTATION
    // =============================================================================

{{#each scenarios}}
    /**
     * Test for scenario: {{title}} ({{id}})
{{#if description}}     * Description: {{description}}
{{/if}}     * Documentation: docs/use-cases/{{../category}}/README.md#{{id}}
     */
    test('{{title}}', () => {
        // =============================================================================
        // START USER IMPLEMENTATION - Feel free to modify the code below this line
        // =============================================================================
        
        // TODO: Implement test for scenario: {{title}}
        //
        // Steps:
{{#each steps}}
        // {{order}}. {{{actor_display acting_actor}}} {{action}}{{#if receiving_actor}} to {{{actor_display receiving_actor}}}{{/if}}
{{/each}}
        
        // Arrange
        // TODO: Set up test data and preconditions
{{#if ../preconditions}}
        // Preconditions:
{{#each ../preconditions}}
        // - {{text}}
{{/each}}{{#if preconditions}}{{#each preconditions}}
        // - {{text}}
{{/each}}{{/if}}
{{else if preconditions}}
        // Preconditions:
{{#each preconditions}}
        // - {{text}}
{{/each}}
{{/if}}
        
        // Act
        // TODO: Execute the scenario steps above
        
        // Assert
        // TODO: Verify the results
{{#if ../postconditions}}
        // Postconditions:
{{#each ../postconditions}}
        // - {{text}}
{{/each}}{{#if postconditions}}{{#each postconditions}}
        // - {{text}}
{{/each}}{{/if}}
{{else if postconditions}}
        // Postconditions:
{{#each postconditions}}
        // - {{text}}
{{/each}}
{{/if}}
        
        throw new Error('Test not implemented yet');
        
        // =============================================================================
        // END USER IMPLEMENTATION - Do not modify anything below this line
        // =============================================================================
    });
    
{{/each}}
});